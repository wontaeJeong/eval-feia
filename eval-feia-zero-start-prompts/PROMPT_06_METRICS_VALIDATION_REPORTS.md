# Prompt 06: Metrics, Validation, and Reports

Implement metrics and validation.

Requirements:

- parse local events as source of truth
- compute trajectory metrics
- find generated AutoGen config
- parse JSON
- validate expected structure
- scan for secrets
- write `summary.json`, `summary.csv`, and `validation.json`

Add tests for validation and CSV/JSON schemas.
